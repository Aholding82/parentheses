# This module parentheses is capable of taking an input string including either opening parentheses
# ' ([{' or '}])' and checking whether the input string e.g '))((())' has all parentheses completely
# closed. 

# Will return either 'valid' or 'not valid'